/* =============================================================
   پیکربندی زمان اجرا — بدون نیاز به ساخت مجدد سایت
   مقادیر null یعنی «هنوز راه‌اندازی نشده» و سایت به‌جای آن‌ها
   متن جایگزین صادقانه نمایش می‌دهد.
   ============================================================= */
window.GR_CONFIG = {
  // شمارهٔ تماس رسمی (مثال: "02191000000" یا "09120000000")
  phone: null,

  // ایمیل رسمی (طبق کتاب برند: info@ghatehresan.ir پس از ثبت دامنه)
  email: null,

  // نشانی انبار/دفتر
  address: null,

  // پیوند شبکه‌های اجتماعی — فقط پس از رزرو قطعی هندل @ghatehresan پر شود
  social: {
    instagram: null,
    telegram: null,
  },

  // نقاط اتصال سرور؛ تا زمانی که Backend واقعی وجود ندارد،
  // فرم‌ها حالت «اتصال برقرار نیست» را نمایش می‌دهند.
  endpoints: {
    partRequest: null, // نمونه: "/api/part-requests"
    contact: null,     // نمونه: "/api/contacts"
  },
};
